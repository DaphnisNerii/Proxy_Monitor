---
wave: 1
depends_on: []
files_modified: [requirements.txt, .gitignore, config_manager.py, monitor_service.py, system_utils.py]
autonomous: true
---

# Plan 01-01: 核心逻辑模块化与环境构建

本计划旨在初始化项目环境，并将 `proxy_monitor.pyw` 中的非 UI 逻辑拆分为独立的、可测试的模块。

## 任务列表

<task>
<read_first>proxy_monitor.pyw</read_first>
<acceptance_criteria>
1. 根目录下存在 `requirements.txt`，包含 Pillow, pystray 等必要库。
2. 存在 `.gitignore` 忽略 __pycache__ 和临时日志。
</acceptance_criteria>
<action>
初始化项目环境文件。确保依赖库版本明确，为后续的 Pre-flight 自动安装提供基准。
</action>
</task>

<task>
<read_first>proxy_monitor.pyw</read_first>
<acceptance_criteria>
1. `config_manager.py` 成功导出 `ProxyConfig` 类。
2. 支持从 Windows 注册表读取 `ProxyEnable` 和 `ProxyServer`。
3. 提供解析订阅 URL 并提取配置的方法。
</acceptance_criteria>
<action>
实现配置管理模块。封装所有注册表 IO 操作，确保逻辑层不再直接调用 `winreg`。
</action>
</task>

<task>
<read_first>proxy_monitor.pyw</read_first>
<acceptance_criteria>
1. `monitor_service.py` 导出 `TrafficMonitor` 类。
2. 能够独立计算流量差值，不依赖任何 UI 变量。
3. 提供回调接口用于状态更新。
</acceptance_criteria>
<action>
实现监控服务模块。剥离流量统计逻辑，使其成为一个纯粹的后台 service。
</action>
</task>

<task>
<read_first>proxy_monitor.pyw</read_first>
<acceptance_criteria>
1. `system_utils.py` 实现基于 `ctypes` 的 `NamedMutex` 单实例检查。
2. 实现 `PreFlightCheck` 类，检查依赖完整性。
3. 如果缺少库，支持调用 `pip install` 进行自修复，并在失败时写入 `error_report.log`。
</acceptance_criteria>
<action>
实现系统工具模块。增强程序的初始化健壮性，防止多开冲突，并提供自愈能力。
</action>
</task>

## 验证标准
1. 运行 `python -m pytest`（如已配置）或手动导入各模块无报错。
2. `system_utils.py` 的单实例测试在模拟运行下表现正常。

## Must Haves
- 注册表操作必须具有异常处理。
- 命名互斥体必须使用 `Global\` 前缀以确保系统范围内唯一。
