---
wave: 2
depends_on: [01-01-PLAN.md]
files_modified: [ui_components.py, tray_app.py, main.pyw]
autonomous: true
---

# Plan 01-02: UI 组件化与入口重组

本计划旨在将 GUI 逻辑与业务逻辑对接，并构建最终的程序入口 `main.pyw`。

## 任务列表

<task>
<read_first>proxy_monitor.pyw</read_first>
<acceptance_criteria>
1. `ui_components.py` 封装了 `SettingsWindow` 类。
2. 窗口样式（Indigo 风格）与逻辑解耦。
3. 支持通过 `after()` 方法在主线程刷新 UI。
</acceptance_criteria>
<action>
重组 Tkinter UI。将所有的样式配置和窗口布局封装为可复用的组件。
</action>
</task>

<task>
<read_first>proxy_monitor.pyw</read_first>
<acceptance_criteria>
1. `tray_app.py` 成功创建 `ProxyTrayIcon` 并在独立线程运行。
2. 支持点击菜单打开设置界面并触发立即刷新。
</acceptance_criteria>
<action>
封装系统托盘应用。实现 Pystray 图标逻辑，并与监控服务建立通信机制。
</action>
</task>

<task>
<read_first>01-01-PLAN.md</read_first>
<acceptance_criteria>
1. `main.pyw` 成为新的唯一入口点。
2. 启动流程：单实例检查 -> Pre-flight 自检 -> 加载配置 -> 启动监控 -> 运行托盘 -> Tkinter Loop。
</acceptance_criteria>
<action>
构建集成入口。编写引导逻辑，将各模块有机结合。
</action>
</task>

## 验证标准
1. 双击运行 `main.pyw` 可成功在托盘显示图标。
2. 右键“设置”能正常弹出配置窗口，且保存后逻辑生效。
3. 试图启动第二个实例时，程序应静默退出。

## Must Haves
- Pystray 代码必须能在子线程安全运行。
- 所有的 `sys.stdout` 重定向必须在 `main.pyw` 的最早期完成。
